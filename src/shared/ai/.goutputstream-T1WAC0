#include "ai.h"
#include "engine.h"
#include "state.h"
#include <iostream>
#include <stdlib.h>
#include <unistd.h>

using namespace ai;
using namespace engine;
using namespace state;
using namespace std;


int DeepIA::run (engine::Engine& engine, sf::RenderWindow& window){
	
	if(state.getPlaying()->getIa()==false) return;
	
	State state = engine.getState();
	
	
	
	
	return 0;
}

int DeepIA::possibleActions (){
	
	Player* playing = state.getPlaying();
	
	for(int d=0; d<4; d++){
		switch(d){
			case
		}
	}
		actionsList
	
}
